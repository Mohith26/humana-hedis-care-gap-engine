# Dev/test/bench deps.
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
# Optional: validates that generated bundles are schema-valid FHIR R4B.
# The FHIR-validation test skips gracefully if this import is unavailable.
fhir.resources>=7.1.0
